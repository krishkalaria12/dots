# Auto-generated by DMS in the live setup. Shipped here so Hyprland boots cleanly
# before DMS writes overrides.

$primary = rgb(a0cafd)
$outline = rgb(8d9199)

general {
    col.active_border = $primary
    col.inactive_border = $outline
}

group {
    col.border_active = $primary
    col.border_inactive = $outline

    groupbar {
        col.active = $primary
        col.inactive = $outline
    }
}
