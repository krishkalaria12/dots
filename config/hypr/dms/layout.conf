# Auto-generated by DMS in the live setup. Shipped here so Hyprland has sane
# defaults until DMS applies its own layout overrides.

general {
    gaps_in = 15
    gaps_out = 15
    border_size = 1
}

decoration {
    rounding = 10
}
